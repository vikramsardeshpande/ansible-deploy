#SOAP enpoint. point this to the location of the cmapi application
soap.endpoint={{ connection_protocol }}://{{ server_http_address }}:{{ server_http_port }}/cms/services/Router
soap.options.enable_rest=true
